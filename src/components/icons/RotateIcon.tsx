import { Icon } from '@chakra-ui/react';

const RotateIcon = (props: any) => (
  <Icon viewBox='0 0 24 24' {...props}>
    <circle fill='currentColor' cx='12' cy='12' r='3' />
  </Icon>
);

export default RotateIcon;
